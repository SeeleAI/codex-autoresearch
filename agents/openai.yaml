interface:
  display_name: "Codex Autoresearch"
  short_description: "Unified entrypoint for environment checks, recovery, and autonomous iteration"
  default_prompt: "Use $codex-autoresearch as the public entrypoint when you need to initialize or recover an autoresearch run, check environment readiness before resuming, or continue ordinary autonomous iteration through the nested env-bootstrap and autoresearch skills."
